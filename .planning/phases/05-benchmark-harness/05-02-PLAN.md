---
phase: 05-benchmark-harness
plan: "02"
type: execute
wave: 3
depends_on: ["05-01"]
files_modified:
  - benchmarks/results/baseline.json
autonomous: false
requirements:
  - BENCH-01
  - BENCH-03
  - BENCH-05

must_haves:
  truths:
    - "`benchmarks/results/baseline.json` exists and contains scenario results for Chromium + Firefox with p50_ms, p75_ms, p99_ms, and mb_s fields"
    - "`node benchmarks/compare.mjs benchmarks/results/baseline.json benchmarks/results/baseline.json --threshold 10` exits 0 (comparing a file with itself is always 0% delta)"
    - "The run establishes the two-browser local baseline that the WASM decision in Plan 03 will analyze"
    - "The local-vs-CI split is documented: local baseline = Chromium + Firefox; full CI baseline (including WebKit) on ubuntu-latest"
  artifacts:
    - path: "benchmarks/results/baseline.json"
      provides: "Committed benchmark baseline — ground truth for CI regression comparisons"
      contains: "scenarios array with mb_s, p50_ms, p75_ms, p99_ms per scenario"
  key_links:
    - from: "benchmarks/results/baseline.json"
      to: "benchmarks/compare.mjs"
      via: "before.json argument in CI bench.yml comparison step"
      pattern: "baseline\\.json"
---

<objective>
Run the full local benchmark suite on Chromium + Firefox, capture the output as `benchmarks/results/baseline.json`, verify the comparator works against it, and commit the baseline to the repository. This is the empirical data that Plan 03 will analyze to write the WASM decision.

Purpose: Without a committed baseline, the CI regression gate has no reference point. This plan closes BENCH-01 (harness runs locally) and BENCH-03 (comparison published) by producing the artifact.
Output: `benchmarks/results/baseline.json` committed to the repository.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-harness/05-CONTEXT.md
@.planning/phases/05-benchmark-harness/05-01-SUMMARY.md
@benchmarks/compare.mjs
@benchmarks/helpers/reporter.ts

<interfaces>
BenchJsonReporter (from benchmarks/helpers/reporter.ts) is registered in vitest.bench.config.ts reporters array.
After `pnpm bench:local`, the reporter writes:
- `benchmarks/results/<timestamp>-local.json`
- `benchmarks/results/baseline.json` (overwrite)

compare.mjs CLI:
```
node benchmarks/compare.mjs <before.json> <after.json> [--threshold N]
Exit 0 = pass. Exit 1 = regression or missing file.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire BenchJsonReporter into vitest.bench.config.ts and run baseline</name>
  <files>vitest.bench.config.ts, benchmarks/results/baseline.json</files>
  <action>
First, update `vitest.bench.config.ts` to include the BenchJsonReporter. Add it to the reporters array at the top-level config (not per-project — the reporter should run once per full bench run):

```typescript
import { defineConfig } from "vitest/config";
import { BenchJsonReporter } from "./benchmarks/helpers/reporter.js";

export default defineConfig({
  test: {
    passWithNoTests: true,
    reporters: [new BenchJsonReporter()],
    projects: [
      // ... existing three projects unchanged ...
    ],
  },
});
```

Then run the local baseline:

```bash
cd /home/sandwich/Develop/iframebuffer
pnpm bench:local
```

This runs Chromium + Firefox only (WebKit excluded locally — ICU ABI mismatch on Arch Linux, established in Phase 1). Expected duration: 2–5 minutes for default payload sizes (1KB, 64KB, 1MB, 16MB).

After the run, verify `benchmarks/results/baseline.json` was written by the reporter.

**Local-vs-CI split documentation (important for the SUMMARY):**
- Local baseline: Chromium + Firefox only (Arch Linux ICU 74 vs 78 ABI mismatch prevents WebKit)
- CI nightly baseline: All three browsers on ubuntu-latest with `--with-deps` (Phase 1 precedent)
- The harness SUPPORTS three browsers (all three projects in vitest.bench.config.ts); the local run skips WebKit by using `pnpm bench:local` which passes `--project=bench-chromium --project=bench-firefox`
- This matches Phase 1 and Phase 3 precedents for the same WebKit-local limitation

Then run the comparator self-check:

```bash
node benchmarks/compare.mjs benchmarks/results/baseline.json benchmarks/results/baseline.json --threshold 10
```

A file compared to itself always produces 0% delta on all metrics — this proves the comparator is wired correctly.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/iframebuffer && node --input-type=module <<'EOF'
import { readFileSync } from 'node:fs';
try {
  const baseline = JSON.parse(readFileSync('benchmarks/results/baseline.json', 'utf8'));
  const hasScenarios = Array.isArray(baseline.scenarios) && baseline.scenarios.length > 0;
  const firstScen = baseline.scenarios[0];
  const hasMetrics = firstScen &&
    typeof firstScen.mb_s === 'number' &&
    typeof firstScen.p50_ms === 'number' &&
    typeof firstScen.p99_ms === 'number';
  console.log({ hasScenarios, hasMetrics, scenarioCount: baseline.scenarios.length, browser: baseline.browser });
  process.exit(hasScenarios && hasMetrics ? 0 : 1);
} catch (e) {
  console.error('baseline.json missing or malformed:', e.message);
  process.exit(1);
}
EOF</automated>
  </verify>
  <done>
    `benchmarks/results/baseline.json` exists with a non-empty `scenarios` array. Each scenario has `mb_s`, `p50_ms`, `p75_ms`, `p99_ms` fields. Comparator self-check exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify baseline.json results are plausible</name>
  <what-built>
    Full local benchmark run on Chromium + Firefox. `benchmarks/results/baseline.json` written by BenchJsonReporter. Comparator self-check passes.
  </what-built>
  <how-to-verify>
    1. Check that `benchmarks/results/baseline.json` exists and contains plausible numbers:
       ```
       cat benchmarks/results/baseline.json | node -e "const d=require('fs').readFileSync('/dev/stdin','utf8'); const j=JSON.parse(d); j.scenarios.slice(0,3).forEach(s=>console.log(s.name, 'mb_s:', s.mb_s.toFixed(1), 'p99:', s.p99_ms.toFixed(1)+'ms'))"
       ```
    2. Verify the binary-transfer library scenarios show higher MB/s than naive-baseline for payloads >= 1 MB (BENCH-03 must-have). If the library is slower than naive for large payloads, the harness has a wiring problem (likely sendViaLibrary is not using the transferable path).
    3. Confirm no scenario shows `mb_s: 0` — that would indicate the payload size extraction in reporter.ts failed to parse the scenario name.
    4. Run comparator self-check manually: `node benchmarks/compare.mjs benchmarks/results/baseline.json benchmarks/results/baseline.json` -- must print "PASS" and exit 0.
  </how-to-verify>
  <action>Review baseline.json output and confirm numbers are plausible (non-zero MB/s, library >= naive for 1MB+ binary). If issues found, fix the harness wiring and re-run pnpm bench:local.</action>
  <verify>
    <automated>node benchmarks/compare.mjs benchmarks/results/baseline.json benchmarks/results/baseline.json --threshold 10</automated>
  </verify>
  <done>baseline.json contains plausible non-zero MB/s numbers; comparator self-check exits 0; human approved.</done>
  <resume-signal>Type "approved" if baseline.json has plausible numbers and comparator self-check passes. Type "issues: [description]" if something looks wrong.</resume-signal>
</task>

</tasks>

<verification>
1. `benchmarks/results/baseline.json` exists with non-empty scenarios array
2. Each scenario has mb_s, p50_ms, p75_ms, p99_ms, samples, rme fields
3. `node benchmarks/compare.mjs baseline.json baseline.json` exits 0
4. binary-transfer library shows >= naive-baseline MB/s for 1 MB+ payloads
5. Baseline committed to git (executor should run `git add benchmarks/results/baseline.json && git commit -m "bench(05): commit initial local baseline (chromium+firefox)"`)
</verification>

<success_criteria>
- `benchmarks/results/baseline.json` committed to the repository
- JSON has plausible throughput numbers (not all zeros, not all Infinity)
- Comparator self-check exits 0
- Human checkpoint approved
- Local-vs-CI split documented in SUMMARY (Chromium+Firefox locally; WebKit on CI ubuntu-latest)
</success_criteria>

<output>
After completion, create `.planning/phases/05-benchmark-harness/05-02-SUMMARY.md`
Include in the summary:
- Actual scenario names and results from baseline.json (copy the top-line numbers)
- The local-vs-CI split note
- Whether library binary-transfer beats naive postMessage at 1 MB+ (BENCH-03 data point)
</output>

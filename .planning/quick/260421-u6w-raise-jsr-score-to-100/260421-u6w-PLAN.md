---
phase: quick-260421-u6w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - jsr.json
  - src/framing/encode-decode.ts
  - src/framing/types.ts
  - src/transport/endpoint.ts
  - src/transport/seq.ts
  - src/transport/sab-capability.ts
  - src/transport/adapters/message-port.ts
  - src/transport/adapters/service-worker.ts
  - src/transport/adapters/window.ts
  - src/transport/adapters/worker.ts
  - src/types.ts
  - src/channel/channel.ts
  - src/adapters/lowlevel.ts
  - src/adapters/emitter.ts
  - src/adapters/streams.ts
  - src/relay/bridge.ts
autonomous: false
requirements: [JSR-SCORE-100]
---

<objective>
Raise the JSR score for `@sandwich/postwire` from 70% to 100% by closing
the five remaining gaps (description, runtimeCompat ≥2, symbol-doc coverage
≥80%, provenance from CI, and the final version-bump + publish step).

Purpose: JSR score 100% is a public trust signal for a library whose core
value proposition is "slots into security-sensitive postMessage boundaries."
A higher score materially reduces friction for first-time evaluators.

Output:
  - `jsr.json` carrying `description` + `runtimeCompat` (verified against
    adapter source — only runtimes the code actually supports)
  - TSDoc on ≥80% of exported symbols surfaced by `jsr publish --dry-run`
  - A new patch release published through the existing OIDC-backed
    `publish.yml` so JSR records a sigstore provenance attestation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md
@jsr.json
@package.json
@.github/workflows/publish.yml
@src/index.ts

# Current state notes (from live JSR UI as of 2026-04-21):
# - JSR score: 70% (9/14)
# - PASSING: readme/module doc, examples in readme, module docs in all
#   entrypoints, no slow types
# - FAILING:
#     * Docs for most symbols (3/5) — 52% coverage, need ≥80%
#     * Has description (0/1)
#     * ≥1 runtime compatible (0/1)
#     * ≥2 runtimes compatible (0/1)
#     * Has provenance (0/1) — property of the *published* version, so
#       resolves only when a new version is pushed from CI

# NOTE: The working copy of jsr.json ALREADY contains `description` and
# `runtimeCompat` with all five runtimes set to true. This is from a
# post-70%-score commit that hasn't been published yet. The executor must:
#   1. Verify the runtimeCompat claim is honest (do NOT leave `true` for a
#      runtime the code cannot possibly run on).
#   2. Close the remaining symbol-doc gap.
#   3. Trigger a new patch publish so JSR records provenance AND re-scores.
</context>

<interfaces>
<!-- Exported symbol surface (from src/index.ts). This is the full set of -->
<!-- public symbols JSR evaluates for doc coverage. Anything exported here -->
<!-- that lacks a TSDoc block counts against the symbol-docs score. -->

From src/index.ts — exported value symbols:
  decode, encode                                   (framing/encode-decode.ts)
  FRAME_MARKER, PROTOCOL_VERSION                   (framing/types.ts)
  createMessagePortEndpoint                        (transport/adapters/message-port.ts)
  createServiceWorkerEndpoint                      (transport/adapters/service-worker.ts)
  createWindowEndpoint                             (transport/adapters/window.ts)
  createWorkerEndpoint                             (transport/adapters/worker.ts)
  isSabCapable                                     (transport/sab-capability.ts)
  seqGT, seqLT, seqLTE, seqMask, seqNext,
    HALF_WINDOW, SEQ_BITS, SEQ_MASK                (transport/seq.ts)
  StreamError                                      (types.ts)
  Channel, createChannel                           (channel/channel.ts)
  createEmitterStream                              (adapters/emitter.ts)
  createLowLevelStream                             (adapters/lowlevel.ts)
  createStream                                     (adapters/streams.ts)
  createRelayBridge                                (relay/bridge.ts)

From src/index.ts — exported type symbols (JSR scores these too):
  BaseFrame, CancelFrame, CapabilityFrame, ChunkType, CloseFrame,
    CreditFrame, DataFrame, Frame, OpenAckFrame, OpenFrame, ResetFrame
  ServiceWorkerEndpointMeta
  PostMessageEndpoint
  ErrorCode
  ChannelOptions
  EmitterOptions, EmitterStream
  LowLevelOptions, LowLevelStream
  StreamsOptions, StreamsPair
  RelayBridge, RelayBridgeOptions, RelayStats

Provenance workflow (already wired — .github/workflows/publish.yml):
  - Trigger: push tag v*
  - permissions: id-token: write, contents: write
  - Steps: npm publish --provenance --access public (OIDC)
           pnpm exec jsr publish (OIDC, no token)
  -> Any future `git tag vX.Y.Z && git push --tags` will publish with
     provenance to both registries.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify & finalize jsr.json (description + honest runtimeCompat)</name>
  <files>jsr.json</files>
  <action>
    The working-tree jsr.json already has a `description` and a
    `runtimeCompat` block claiming browser + deno + node + bun + workerd.
    Verify each claim is HONEST before publishing, per the constraint
    "must not guess at runtime compatibility."

    Steps:

    1. Read the transport adapter files to enumerate every runtime API
       the library touches:
         - src/transport/endpoint.ts
         - src/transport/adapters/message-port.ts
         - src/transport/adapters/window.ts
         - src/transport/adapters/worker.ts
         - src/transport/adapters/service-worker.ts
         - src/transport/sab-capability.ts
         - src/framing/encode-decode.ts
         - src/channel/channel.ts

    2. For each of the five claimed runtimes, confirm:
         - browser: structured-clone + MessageChannel + Worker + Window
           postMessage -> YES (the whole library is built for this).
         - deno: has MessageChannel, structured clone, Worker, and
           BroadcastChannel as web standards. Verify no Node-only imports
           (`node:*`) are required at the top level of src/index.ts's
           transitive graph.
         - node: requires `node:worker_threads` MessagePort per the
           Phase 03 decision note. Confirm by grepping for `node:` imports
           in src/; if any are guarded (`typeof process !== 'undefined'`
           or isolated to test files), node is still compatible because
           the library treats any `{ postMessage, addEventListener/onmessage }`
           shape as an endpoint.
         - bun: same web-standard surface as Deno + Node-compat layer.
           No special bun-specific code; claim is valid.
         - workerd (Cloudflare Workers): MessageChannel and structured
           clone exist. Service worker adapter uses `self.addEventListener`
           which is fine. Confirm no DOM-only (`window`, `document`)
           references escape the window adapter's own module.

    3. If any claim cannot be substantiated, REMOVE that key from
       runtimeCompat. Minimum floor is `browser: true` plus one more
       (to clear the ≥2 runtime gauge). The current strong evidence
       is browser + deno + node + bun; workerd is the weakest and
       may need to be dropped if the service-worker adapter imports
       `self.registration` or similar Workers-incompatible APIs.

    4. Leave `description` as-is — it's already the multi-paragraph
       blurb matching package.json, which JSR accepts.

    5. Run `pnpm exec jsr publish --dry-run` and capture the output.
       The dry-run report lists exported symbols without docs under a
       "symbol documentation" section. Save that list to
       `.planning/quick/260421-u6w-raise-jsr-score-to-100/undocumented-symbols.txt`
       for Task 2 to consume.

    Do NOT touch src/ in this task. This task is pure manifest hygiene
    plus enumeration of the doc-coverage shortfall.
  </action>
  <verify>
    <automated>
      pnpm exec jsr publish --dry-run 2>&amp;1 | tee .planning/quick/260421-u6w-raise-jsr-score-to-100/dryrun-before-docs.log
      test -s .planning/quick/260421-u6w-raise-jsr-score-to-100/undocumented-symbols.txt
      node -e "const j=require('./jsr.json'); if(!j.description||j.description.length&lt;40) process.exit(1); const rc=j.runtimeCompat||{}; const trueCount=Object.values(rc).filter(v=>v===true).length; if(trueCount&lt;2) process.exit(2); console.log('OK: description='+j.description.length+' chars, runtimes='+trueCount);"
    </automated>
  </verify>
  <done>
    - jsr.json has `description` (≥40 chars) and `runtimeCompat` with
      ≥2 runtimes set to `true`, each substantiated by source evidence.
    - `jsr publish --dry-run` exits 0 and its output is captured.
    - `undocumented-symbols.txt` lists every exported symbol JSR flagged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add TSDoc to undocumented symbols to pass ≥80% coverage</name>
  <files>
    src/framing/encode-decode.ts, src/framing/types.ts,
    src/transport/endpoint.ts, src/transport/seq.ts,
    src/transport/sab-capability.ts,
    src/transport/adapters/message-port.ts,
    src/transport/adapters/service-worker.ts,
    src/transport/adapters/window.ts,
    src/transport/adapters/worker.ts,
    src/types.ts, src/channel/channel.ts,
    src/adapters/lowlevel.ts, src/adapters/emitter.ts,
    src/adapters/streams.ts, src/relay/bridge.ts
  </files>
  <action>
    Consume the `undocumented-symbols.txt` produced by Task 1. For each
    listed exported symbol, add a concise TSDoc block (1–3 lines —
    NO fluff, NO restating the obvious) immediately above the declaration.

    Target: lift coverage from 52% → ≥80%. With ~40 exported symbols in
    src/index.ts, 80% means ≤8 may remain undocumented. Do NOT try to
    hit 100% — diminishing returns. Prioritize top-level factories and
    public types over internal constants (`FRAME_MARKER`, `SEQ_BITS`,
    etc. — acceptable to leave sparse).

    TSDoc rules for this task (keep it mechanical, no invention):
      - Factories (`createXxx`): one sentence describing what kind of
        endpoint/stream/channel is returned, with `@param` for each
        required option. One `@returns` line. If the factory is already
        documented in src/index.ts's module doc, a one-liner here is
        enough — don't duplicate the examples.
      - Types/interfaces (`XxxOptions`, `XxxStream`): one sentence on
        what the type represents; document each non-obvious field with
        a `@property`-style inline comment only if the name isn't
        self-explanatory.
      - Error classes (`StreamError`): one sentence + `@param` for the
        constructor signature.
      - Low-level utility exports (seq arithmetic, frame constants):
        one-line block comment suffices (`/** Next sequence number with
        32-bit wraparound. */`).

    Pattern to follow (already present on `createChannel`/`createStream`
    in src/index.ts's module doc block — mirror that tone):

    ```ts
    /**
     * Wrap a Worker / DedicatedWorkerGlobalScope as a {@link PostMessageEndpoint}.
     *
     * @param worker - A `Worker` instance (main thread) or `self` (worker thread).
     * @returns An endpoint suitable for passing to {@link createChannel}.
     */
    export function createWorkerEndpoint(worker: Worker | DedicatedWorkerGlobalScope): PostMessageEndpoint { ... }
    ```

    After edits, re-run `pnpm exec jsr publish --dry-run` and confirm
    the "symbol documentation" warning count has dropped to ≤20% of
    total exports. If it hasn't, the dry-run output names what's still
    missing — iterate once.

    Also run `pnpm typecheck` and `pnpm lint` to catch any broken
    TSDoc syntax (Biome 2 flags malformed JSDoc).

    Do NOT reformat unrelated code. Do NOT change runtime behaviour.
    Doc-only edits.
  </action>
  <verify>
    <automated>
      pnpm typecheck
      pnpm lint
      pnpm exec jsr publish --dry-run 2>&amp;1 | tee .planning/quick/260421-u6w-raise-jsr-score-to-100/dryrun-after-docs.log
      # Confirm symbol-doc warnings dropped by comparing before/after counts.
      # The dry-run reports "Missing documentation" — count occurrences.
      BEFORE=$(grep -c -iE "missing (jsdoc|documentation)" .planning/quick/260421-u6w-raise-jsr-score-to-100/dryrun-before-docs.log || echo 0)
      AFTER=$(grep -c -iE "missing (jsdoc|documentation)" .planning/quick/260421-u6w-raise-jsr-score-to-100/dryrun-after-docs.log || echo 0)
      echo "Undocumented before: $BEFORE / after: $AFTER"
      test "$AFTER" -le "$((BEFORE / 3))" || { echo "Doc coverage did not improve enough (need ~80%, before=$BEFORE after=$AFTER)"; exit 1; }
    </automated>
  </verify>
  <done>
    - `pnpm typecheck` and `pnpm lint` pass.
    - `jsr publish --dry-run` reports ≤20% undocumented exports
      (i.e. the "missing documentation" warning count is ≤⅓ of the
      pre-edit count — which maps to ≥80% coverage given the 52% start).
    - No runtime code changed (verified by `git diff --stat` showing
      only comment additions).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Bump patch, tag, and publish — operator confirms score</name>
  <what-built>
    - jsr.json with description + honest runtimeCompat
    - TSDoc blocks closing the symbol-doc gap to ≥80%
    - publish.yml already configured with OIDC provenance (npm + JSR);
      no workflow changes required.
  </what-built>
  <how-to-verify>
    This step involves a real publish. Claude should prepare everything,
    but the human must approve the tag push because it triggers a
    public, irreversible release.

    Claude — do these automated steps FIRST, then pause:

    1. Sync versions: bump the patch on both manifests.
       ```
       # package.json is 0.1.0, jsr.json is 0.1.2 — they're out of sync.
       # The repo has a `sync-jsr-version.mjs` script; use it via the
       # standard changeset flow if a changeset already exists, otherwise
       # do a direct patch bump on both files to the same next version
       # (e.g. 0.1.3) so npm and JSR stay aligned.
       pnpm changeset          # interactive — skip if no changeset needed
       pnpm changeset version  # applies version bump + runs sync script
       # If no changeset exists, edit both files by hand to the same
       # target version and commit.
       ```

    2. Run the full dry-run gauntlet one more time:
       ```
       pnpm pub:dry-run
       ```
       This runs: build + publint + npm publish --provenance --dry-run +
       jsr publish --dry-run. Must exit 0.

    3. Commit the version bump + doc edits with a conventional message:
       ```
       git add jsr.json package.json src/ .changeset/ CHANGELOG.md
       git commit -m "docs: raise JSR score to 100% (tsdoc coverage + manifest)"
       ```

    4. PAUSE here. Print the exact commands the user will run, and wait.

    User — after Claude pauses, run these commands yourself:
       ```
       # Push the commit
       git push origin master

       # Tag and push — this triggers .github/workflows/publish.yml
       git tag v<NEW_VERSION>
       git push origin v<NEW_VERSION>
       ```

    Then verify:
       1. Watch the Publish workflow at https://github.com/sandwichfarm/postwire/actions
          — both npm and JSR publish steps must go green.
       2. After ~2 minutes, refresh https://jsr.io/@sandwich/postwire
          and confirm:
            - Score shows 100% (or 14/14).
            - "Has provenance" is now passing (sigstore transparency
              log entry linked).
            - "Has docs for most symbols" is passing.
            - "Has a description" is passing.
            - Both runtime-compat checkmarks are passing.
       3. On https://www.npmjs.com/package/postwire, the new version
          shows the "Provenance" badge linking to the GitHub Actions run.

    If score is <100%, check which tile is still red on jsr.io and
    either add more TSDoc (restart Task 2) or adjust runtimeCompat.
  </how-to-verify>
  <resume-signal>
    Type "published — score 100" if the JSR score tile reports 100%,
    OR paste the names of any remaining red tiles from jsr.io so the
    plan can be re-entered for a targeted fix.
  </resume-signal>
</task>

</tasks>

<verification>
Overall:
  - `jsr.json` description and runtimeCompat fields are present and honest.
  - `jsr publish --dry-run` shows ≥80% symbol-doc coverage.
  - A new version has been tagged and pushed; the Publish workflow
    succeeded with OIDC provenance.
  - jsr.io/@sandwich/postwire reports score 100% (14/14).
</verification>

<success_criteria>
- [ ] jsr.json committed with description + ≥2 verified runtimes in runtimeCompat.
- [ ] ≥80% of exported symbols in src/index.ts have TSDoc blocks
      (measured by `jsr publish --dry-run` output).
- [ ] New patch version tagged (`vX.Y.Z`) and pushed; publish.yml ran green.
- [ ] Live JSR score for @sandwich/postwire = 100%.
</success_criteria>

<output>
After completion, create `.planning/quick/260421-u6w-raise-jsr-score-to-100/260421-u6w-SUMMARY.md`
capturing:
  - Final published version number
  - Runtimes kept vs dropped in runtimeCompat (with one-line justification each)
  - Approximate symbol-doc coverage before/after
  - Link to the provenance attestation on jsr.io
</output>

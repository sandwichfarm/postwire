{
  "name": "postwire",
  "version": "0.1.0",
  "type": "module",
  "description": "High-throughput, reliable, ordered stream abstraction over any postMessage boundary — iframes, web workers, service workers, MessageChannel, BroadcastChannel — with credit-based backpressure, three API surfaces (low-level, Node EventEmitter, WHATWG Streams), multi-hop relay, optional multiplexing, and feature-detected SharedArrayBuffer fast path. Zero runtime dependencies, strict-CSP compatible baseline.",
  "keywords": [
    "postmessage",
    "stream",
    "streaming",
    "backpressure",
    "iframe",
    "worker",
    "web-worker",
    "service-worker",
    "messagechannel",
    "broadcastchannel",
    "transferable",
    "sharedarraybuffer",
    "sab",
    "relay",
    "multiplex",
    "whatwg-streams",
    "readablestream",
    "writablestream",
    "eventemitter"
  ],
  "license": "MIT",
  "author": "Sandwich Farm LLC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sandwichfarm/postwire.git"
  },
  "homepage": "https://github.com/sandwichfarm/postwire#readme",
  "bugs": {
    "url": "https://github.com/sandwichfarm/postwire/issues"
  },
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./wasm": {
      "types": "./dist/wasm.d.ts",
      "import": "./dist/wasm.js"
    }
  },
  "files": [
    "dist",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsdown",
    "lint": "biome check . && publint",
    "format": "biome format --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:local": "playwright test --project=chromium --project=firefox",
    "bench": "vitest bench --config vitest.bench.config.ts --outputJson benchmarks/results/latest.json && tsx benchmarks/cpu-profile.mjs && node benchmarks/normalize.mjs benchmarks/results/latest.json benchmarks/results/baseline.json",
    "bench:fast": "vitest bench --config vitest.bench.config.ts --outputJson benchmarks/results/latest.json && node benchmarks/normalize.mjs benchmarks/results/latest.json benchmarks/results/baseline.json",
    "bench:cpu": "tsx benchmarks/cpu-profile.mjs",
    "bench:raw": "vitest bench --config vitest.bench.config.ts --outputJson benchmarks/results/latest.json",
    "bench:heavy": "PW_BENCH_HEAVY=1 vitest bench --config vitest.bench.config.ts --outputJson benchmarks/results/latest.json && node benchmarks/normalize.mjs benchmarks/results/latest.json benchmarks/results/baseline.json",
    "bench:compare": "node benchmarks/compare.mjs",
    "typecheck": "tsc --noEmit",
    "ci": "pnpm build && pnpm lint && pnpm test && pnpm test:e2e",
    "tree-shake:check": "node scripts/tree-shake-check.mjs",
    "version": "changeset version && node scripts/sync-jsr-version.mjs",
    "release": "pnpm build && changeset publish",
    "pub:check": "node scripts/check-name-availability.mjs",
    "pub:dry-run": "pnpm build && pnpm exec publint && npm publish --provenance --dry-run && pnpm exec jsr publish --dry-run"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "0.18.2",
    "@biomejs/biome": "2.4.12",
    "@changesets/cli": "2.31.0",
    "@playwright/test": "1.59.1",
    "@types/node": "25.6.0",
    "@vitest/browser": "4.1.4",
    "@vitest/browser-playwright": "4.1.4",
    "@vitest/coverage-v8": "4.1.4",
    "fast-check": "^4.7.0",
    "jsr": "^0.14.3",
    "publint": "0.3.18",
    "tinybench": "6.0.0",
    "tsdown": "0.21.9",
    "tsx": "4.21.0",
    "typescript": "6.0.3",
    "vite": "^8.0.9",
    "vitest": "4.1.4"
  }
}
